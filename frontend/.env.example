# URL do backend. Em dev local, pode deixar sem essa variável (usa
# http://localhost:3333 por padrão). Em produção, aponte pro backend publicado.
NEXT_PUBLIC_API_URL="https://seu-backend.onrender.com"
